import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Bleu par défaut de MUI
    },
    secondary: {
      main: '#f50057', // Rose par défaut de MUI
    },
  },
});

export default theme;
